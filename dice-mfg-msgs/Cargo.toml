[package]
name = "dice-mfg-msgs"
version = "0.2.1"
edition = "2021"

[dependencies]
corncobs = "0.1.3"
hubpack = "0.1"
serde = { version = "1", default-features = false, features = ["derive"] }
serde-big-array = "0.4"
thiserror = { version = "1.0.40", optional = true }
zerocopy = "0.6"

[features]
std = ["thiserror"]
